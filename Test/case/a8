struct point {
    int x, y;
};

float dotmultiply(struct point A, struct point B)
{
    return A.x * B.y - A.y * B.x;
}

int main()
{
    struct point p1, p2;
    dotmultiply(p1, p2);
    return 0;
}
